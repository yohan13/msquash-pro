# M Squash – Clean Build v2 (Node 16 OK)

Fixes:
- ✅ Ajout des endpoints **/api/blocks** (POST/DELETE) → corrige l’erreur `block_failed`
- ✅ Suppression de tous les `alert()` (UX soft avec bannière)
- ✅ Layout revu (grille plus compacte, boutons ajustés)

## Démarrage
Backend
```bash
cd backend && npm install && npm start
```
Frontend
```bash
cd frontend && npm install && npm run dev
```
